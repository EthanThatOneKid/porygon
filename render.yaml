services:
  - type: web
    name: porygon
    runtime: docker
    plan: free
    healthCheckPath: /healthz
    envVars:
      - key: DISCORD_TOKEN
        sync: false
      - key: LETTA_API_KEY
        sync: false
      - key: LETTA_AGENT_ID
        sync: false
